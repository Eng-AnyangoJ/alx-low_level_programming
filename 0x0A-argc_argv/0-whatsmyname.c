#include "main.h"
#include <stdio.h>
/**
 * main - program that prints its nam
 *
 * @argc: argument count
 * @argv: arguments array
 *
 * Return: (0) SUCCESS
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
